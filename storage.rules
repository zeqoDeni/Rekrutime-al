rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function signedIn() {
      return request.auth != null;
    }

    match /orgs/{orgId}/candidates/{candidateId}/resumes/{fileName} {
      allow read, write: if signedIn();
    }
  }
}
